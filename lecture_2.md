# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним хэш коммита, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba

можно в мастер слить сразу несколько веток(но только в гите)  
при сливании создается merge commit, в коммите будут родители кол-во слившихся веткок + мастер(просто прикол)

git commit -a -m "Afiget" - делает коммит без добавления в индекс(то что будет в коммите останется там навсегда😈) В итоге все равно хотя бы 1 раз надо git add

git branch -d не влияет на merge

fast-forward слияние - это слияние без коммитов то есть если не было изменения в ветке в которую мы сливаем, то есть просто мы меняем feature-branch на master(меняем коммит на кот указывает master на тот кот в f-b)


\
\\
\
\
\
\
\
\\\\\
\
\\
\
\
\
\
\
\
\

\
\
































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































/# GIT
## /oblects
git lfs - позволяет хранить большие файлы
---
еще контроли версий?
VCS : 
1) централизованная(svn, cvs) - есть отдельный центральный сервер, где есть все. Если отключимся от сервера, не будет ничего
2) распределенная(git, mercurial) - мы клонируем репозиторий, то есть есть информация, которая хранится на ПК

- svn - хранит только изменения
- git - хранит весь файлик

---
git add - добавление в индекс  
index находится в .git - текущее состояние репозитория  
/ba - хэши файлов(хранятся те, которые добавили в index)  
при еще add ничо не перехаписывается - просто добавляетс еще одна папка с другим хэшем  
git cat-file - показывание файлов по хэшу  
при коммите добавляются еще папки с хэшем - то есть создается еще (коммитный)файл который мы только что добавили  
файл коммита содержит информацию о коммите  
коммит ссылается еще на хэш - в этом хэше находится состояние репозитори на момент коммита, в то же время он   ссылается на хэши исходного кода  
git проходит по tree и если не видит файлик то говорит але тут нет файлв в index  

---
точнее будет говорить что мы пишем начало папки + сам хэш
---

у первого коммита нет родителя(  
у всех остальных будет parent(другой коммит)  
дерево с родителем ссылается на файлы в системе  
командой checkout не удаляет хэши, просто переключается между деревом  
файлы, коммиты, дерево - все это файлы  
---
## /refs
ветка это просто файлик с хэшом коммита(любого)  
мы вообще можем ручками добавить ветку в refs, добавить туда хэш коммита и получить то же самое как при git checkout -b artibranch  

ветки могут указывать на один и тот же коммит  

где указано на какой ветке мы сейчас? В HEAD находится просто ссылка на файл ветки(/refs/heads/master)  
новые ветки ссылаются на последний коммит  
если мы перетскиваем голову на коммит(не ветку) то detached head(она будет ссылаться на хэш коммита)  
если мы будет коммитить в таком положении то все наи изменения будут будут потеряны, то есть хэш коммита есть, но как найти его?  
но если помним коммит, то мы можем сделать на нем ветку  

git log --graph --all --decorate -- oneline - отображение графа коммитов красивенько в консоли  

aboba





































































































































































































































































